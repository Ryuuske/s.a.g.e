
---

# Per-repo CLAUDE.md
#
# This file lives at <repo>/.claude/CLAUDE.md in the destination repository.
# It overrides or extends ~/.claude/CLAUDE.md (the global config) for this
# specific project only, per the §10 config-layering convention.
#
# Per ADR-0008 (source-vs-destination separation), S.A.G.E. itself does NOT
# use this stub — it applies only to destination repos that S.A.G.E. is
# installed into. Hand-author your project-specific overrides below.

## Project-specific overrides

<!-- Add project-specific rules, tech stack notes, or §10 overrides here.
     Only write what differs from the global ~/.claude/CLAUDE.md.
     Do not restate global rules verbatim. -->

# See ADR-0013 for context on why this file exists and how it is bootstrapped.
