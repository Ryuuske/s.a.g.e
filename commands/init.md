---
description: Set up S.A.G.E. — install the package, initialize a nook, configure MCP server, and verify everything works.
allowed-tools: Bash, Read, Write, Edit, Glob, Grep
---

Invoke the generic S.A.G.E. skill (using the Skill tool) with the `init` command, then follow its instructions.
