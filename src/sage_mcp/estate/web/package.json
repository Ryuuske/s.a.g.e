{
  "name": "@sage/estate-web",
  "version": "0.0.1",
  "private": true,
  "description": "Sage Estate Dashboard — TypeScript layout engine + SVG renderer workspace",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "vite build"
  },
  "_securityNotes": {
    "GHSA-5xrq-8626-4rwp": "RESOLVED 2026-06-07: bumped vitest 1.6 -> 4.1 (patched floor for CVE-2026-47429, CVSS 9.8). Config migrated off the removed environmentMatchGlobs API to a per-file `// @vitest-environment` docblock."
  },
  "devDependencies": {
    "@types/three": "^0.184.1",
    "happy-dom": "^20.10.2",
    "three": "^0.184.0",
    "tsx": "^4.19.0",
    "typescript": "^5.4.0",
    "vite": "^8.0.16",
    "vite-plugin-singlefile": "^2.3.3",
    "vitest": "^4.1.0"
  }
}
