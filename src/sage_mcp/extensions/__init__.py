"""sage extensions.

Modules under this package implement first-release additions that sit
on top of the core read/write/search surface — agent-keyed drawer
metadata serialisation, wing registry validation, and similar helpers.
Keeping them in their own namespace makes the boundary between core
storage and orchestration-facing extensions explicit.
"""
